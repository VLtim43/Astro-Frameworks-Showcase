---
const { title, bodyColor, headerColor, footerColor, headerTextColor, footerTextColor } = Astro.props;

---

<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="UTF-8" />
		<meta name="description" content="Astro description">
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class={`flex flex-col h-full m-0 `}>
	<header class={`${headerColor} ${headerTextColor} text-center p-4`}>
		<img src="/astroTitleLogo.svg" alt="Astro Logo" class="mb-2 mt-2"/>
	</header>
	
	<main class={`${bodyColor} flex-1`}>
		<slot />
	</main>
	
	<footer class={`${footerColor} ${footerTextColor} text-center p-4`}>		
		<p class="text-sm">© 2023 Your Project Name. All rights reserved.</p>
	</footer>
	
</body>
</html>
